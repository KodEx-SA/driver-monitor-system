"""
event_logger.py

Responsibility (Step 7): persist fatigue events to SQLite (data/logs.db).
The dashboard reads from here — it never touches the live camera pipeline.

Not yet implemented — built in Step 7.
"""
