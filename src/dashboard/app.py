"""
Responsibility (Step 8): Streamlit dashboard. Reads the event log only
trends, session history, CSV export. Runs as a separate process from the
live monitor (src/main.py) so it never competes with the camera loop.
"""
